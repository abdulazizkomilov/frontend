import type { ToastType } from '../../../../components/Toast/types';
import type { DefineComponent } from 'vue';
export declare type ToastItem = {
    time: number;
    type: ToastType;
    text: string;
    component?: DefineComponent;
    componentProps?: Record<string, unknown>;
};
export declare type ToastItemWithId = ToastItem & {
    id: string;
};
export declare type ToastTransition = 'slide-left' | 'slide-right' | 'fade' | 'slide-top' | 'slide-bottom';
export declare type UseToastInjection = {
    add: (toast: ToastItem) => string;
    remove: (id: string) => boolean;
    pop: () => string;
};
