export declare type tAccordionMode = 'flush' | 'alwaysOpen' | 'default';
export declare type tAccordionPanel = {
    order: number;
    id: string;
    isVisible: boolean;
};
declare type tAccordionPanels = {
    [key: string]: tAccordionPanel;
};
declare type tStateElement = {
    id: string;
    flush: boolean;
    alwaysOpen: boolean;
    panels: tAccordionPanels;
};
export declare type tState = {
    [key: string]: tStateElement;
};
export {};
